--https://docs.python.org/3/library/functions.html#abs

zip = zip or require("zip")
pytype = pytype or require("type")
tuple = tuple or require("tuple")
str = str or require("str")
staticmethod = staticmethod or require("staticmethod")
slice = slice or require("slice")
range = range or require("range")
pyobj = pyobj or require("pyobj")
op_in = op_in or require("operator_in")
None = None or require("none")
list = list or require("list")
len = len or require("len")
iter_obj_creator = iter_obj_creator or require("iter_obj_creator")
isinstance = isinstance or require("isinstance")
helper_functions = helper_functions or require("helper_functions")
enumerate = enumerate or require("enumerate")
dict = dict or require("dict")
del = del or require("del")
class = class or require("class")
bool = bool or require("bool")
any = any or require("any")
all = all or require("all")
abs = abs or require("abs")
ascii = ascii or require("ascii")
bin = bin or require("bin")
bytearray = bytearray or require("bytearray")
bytes = bytes or require("bytes")
callable = callable or require("callable")
chr = chr or require("chr")
classmethod = classmethod or require("classmethod")
compile = compile or require("compile")
complex = complex or require("complex")
delattr = delattr or require("delattr")
dir = dir or require("dir")
divmod = divmod or require("divmod")
eval = eval or require("eval")
exec = exec or require("exec")
filter = filter or require("filter")
float = float or require("float")
format = format or require("format")
frozenset = frozenset or require("frozenset")
getattr = getattr or require("getattr")
globals = globals or require("globals")
hasattr = hasattr or require("hasattr")
hash = hash or require("hash")
help = help or require("help")
hex = hex or require("hex")
id = id or require("id")

is_pyobj = is_pyobj or helper_functions.is_pyobj